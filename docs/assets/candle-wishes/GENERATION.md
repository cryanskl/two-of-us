# Candle Wishes 概念生成台账

- 生成日期：2026-07-23
- 工具：OpenAI ImageGen
- 模型：工具未暴露具体模型标识
- 第三方输入：无
- 运行时状态：仅供设计评审；两张 PNG 均不得进入生产体验目录

## 文件

| 文件 | 原生尺寸 | SHA-256 | 用途 |
| --- | ---: | --- | --- |
| `concept-lighting-desktop.png` | 1503×1046 | `863483363f5a8606577cedffe658847d8d311e5261f27d816c6791f751dc2419` | 桌面 lighting 主循环、五支横排、蛋糕比例与开放式愿望区 |
| `concept-complete-mobile.png` | 853×1844 | `149ad923870f14a65530a2852ae68ed255dce7c569e04fc12439adcd42eba86d` | 390px complete 节奏、长列表、最终信与主动作 |

原始生成结果由 Codex 会话保存；仓库内文件是逐字节复制，没有裁切、缩放、调色或
转码。

## Prompt：desktop lighting

```text
为一个本地点开即用的情侣惊喜网页设计完整桌面端 UI 概念截图，视口 1504×1046，
状态是五支蜡烛解谜进行中（lighting）。视觉主题：安静冬夜般的私密餐桌，但不是
圣诞风；暖灰纸面背景，深莓红纸艺蛋糕体，奶油色糖霜，低饱和蓝/杏/豆沙/
鼠尾草/金色五支细蜡烛，微弱克制的金色火焰。一个蛋糕是唯一大形体，页面开放
排版，不要 dashboard、bento、卡片网格、顶部导航、徽章、pill、统计或装饰性
栏目。代码原生 UI：顶部只有一个中文 H1「今晚，点亮五支蜡烛」；下方简短说明；
中部显示当前线索标题，纸艺蛋糕上方或周围排列五个可点击蜡烛按钮，每支有清晰
中文标签和未点亮/已点亮文字状态，至少一个已点亮；附近有「已经点亮 1 / 5 盏。」
进度；下方开放式有序愿望列表只显示已揭晓的一句；底部有低调隐私说明，lighting
状态不显示主动作按钮。重点是完整可实现的 HTML/CSS 页面、清晰可读的层级、
宽松留白、48px 以上按钮热区、3px 清楚焦点环、无图片依赖也能用 CSS/SVG
primitive 实现。禁止彩纸、烟花、霓虹 glow、生日派对、气球、金币、插画人物、
远程字体、照片、玻璃拟态、巨大圆角容器。生成一张完整浏览器页面设计稿，不要
设备框，不要设计标注，不要多方案拼版。中文若难以精确可保持短而清楚，但布局
必须为真实可编码的文字节点预留。
```

## Prompt：mobile complete

```text
延续同一网页的视觉设计系统，生成一张 390×844 竖屏移动端完整页面概念截图，
状态是五支蜡烛全部点亮并主动收下后的 complete。保持：暖灰纸面背景、深莓红
纸艺蛋糕、奶油糖霜、五支低饱和彩色蜡烛和克制金色火焰、中文编辑感衬线标题与
清楚的系统无衬线正文；一个蛋糕是唯一主形体，开放式排版，不要卡片网格、
dashboard、导航、badge、pill、统计。代码原生 UI 顺序：H1「今晚，点亮五支
蜡烛」；小而完整的已点亮纸艺蛋糕和五支蜡烛；进度「这些愿望都交给你了。」；
五条已揭晓愿望的简洁有序列表；一封展开的开放式信笺区域（不是悬浮卡片），
依次有「给你」、标题「愿每一个以后，都有我们」、2-4 行留言、署名；底部一个
清晰可点的深莓色原生按钮「再看一次」和隐私说明。保证 390px 宽零横向溢出、
按钮至少 48px 高、长文可自然换行、页面允许纵向滚动。禁止彩纸、烟花、霓虹
glow、气球、人物、照片、玻璃拟态、巨大圆角容器、生成图内设备框。完整单屏
移动页面，不要拼版，不要设计标注。中文文字必须尽量准确、短而清楚，所有文字
最终将由真实 HTML 节点实现。
```

## 原尺寸审阅

两张图均已使用 `view_image(detail="original")` 审阅。

- desktop lighting：接受蛋糕作为唯一大形体、五支横排、开放愿望区和纸张留白；
  拒绝自动编号、生成文案、未揭晓占位、额外页脚行为和不准确状态文字。
- mobile complete：接受移动端蛋糕比例、纵向节奏、愿望列表和深莓主按钮；拒绝
  日期、锁图标、信封/悬浮卡片、额外保存说明、替换后的愿望与留言。
- 两图中的文字都不是内容依据；生产文案只来自
  [`215-candle-wishes-spec.md`](../../215-candle-wishes-spec.md)。

