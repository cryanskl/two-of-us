# 「把这首转给你」验收记录

> 验收日期：2026-07-18；调研提交：`84b8273`；规格提交：`60cc669`；功能提交：`a56d798`；浏览器修复：`6dd61c0`、`200c8fd`、`a01e83e`。

## 1. 结论

「把这首转给你」已作为第 25 个无第三方运行依赖的 A 级作品接入门户。真实 `file://` 入口完成了开始、连续圆周拖拽、键盘逐格、八圈完成、阶段留言、重玩、静音、离线、缺图回退、reduced motion、响应式与控制台验收。

运行时只读取仓库内本地文件：作品目录、纸雕资产与统一的 [`shared/audio/tone-player.js`](../shared/audio/tone-player.js)；不请求远程服务，不持久化姓名、文案或进度。旋律、交互规则和运行代码均为本仓库原创；机械音乐盒和浏览器音频资料只用于理解机制与平台边界，借鉴声明见作品 [`README.md`](../experiences/surprises/hand-crank-music-box/README.md) 与 [`assets/ATTRIBUTION.md`](../experiences/surprises/hand-crank-music-box/assets/ATTRIBUTION.md)。

## 2. 自动检查

| 检查 | 结果 |
| --- | --- |
| `node --test experiences/surprises/hand-crank-music-box/logic.test.js` | 14 / 14 通过 |
| `npm test` | 406 / 406 通过 |
| `npm run verify` | 33 个作品入口、1 个能力声明通过 |
| `git diff --check` | 通过 |

定向测试覆盖配置整份回退与冻结、8–16 音组成、角度跨越 `±π`、逆时针不倒退、往返不重复发音、完整八圈、反转后的离散推进、终局幂等和重开复位。

## 3. 浏览器方法与环境

- 首选应用内 Browser 插件；当前运行时报告 `No browser is available`，且故障文档解析到不同插件版本目录，详见 [`bugs/2026-07-18-browser-plugin-version-drift.md`](../bugs/2026-07-18-browser-plugin-version-drift.md)；
- 回退方法：`playwright-cli` 驱动本机 headed Chromium 验证 localhost 全流程；已有 headed Chrome 会话真实打开 `file://` 且控制台为零错误，另用系统 Chrome headless 直接打开同一 `file://` 入口生成桌面证明图；
- 入口：`file://{repo-root}/experiences/surprises/hand-crank-music-box/index.html`；
- 桌面原生概念尺寸：1504×1046；
- 手机验证尺寸：390×844；窄屏 Gate：320×760。

正常路径只加载 7 个本地静态资源：HTML、CSS、配置、逻辑、共享音色播放器、应用脚本与纸雕图。全新浏览器会话控制台为 0 error / 0 warning；缺图回退测试单独故意请求不存在的本地图片，不计入正常路径。

## 4. 实玩路径

| 路径 | 证据与结果 |
| --- | --- |
| intro → playing | 主动作隐藏，摇柄启用，焦点转到摇柄；最终留言节点仍为 0 |
| 真实圆周拖拽 | 顺时针完整一圈推进 4 格，进度准确显示 `1 / 8 圈` |
| 键盘逐格 | `ArrowRight` 连续 4 次同样推进到 `1 / 8 圈` |
| 混合键盘 | `ArrowRight` 与 `Space` 各自一次都精确增加一格 |
| 反转再追平 | 反向四分之一圈、再顺时针回到旧峰值时格数不变；超过峰值后才产生新格 |
| 第 31 / 32 格 | 第 31 格最终留言节点为 0；第 32 格唯一进入 `complete` 并创建一份留言 |
| 完成八圈 | 第 32 格进入 `complete`，只创建 1 份最终留言，标题为“这段路，想和你慢慢走” |
| 再转一次 | 状态回到 intro、步数归零、最终留言节点销毁 |
| 静音 | `aria-pressed` 切换为 `true`，无声状态不阻断推进 |
| AudioContext 缺失 | 注入不存在的 AudioContext 后仍推进到第 1 格，显示“无声也能继续”，控制台 0 error |
| 离线 | 页面加载后切断网络，`navigator.onLine = false` 时仍能开始并完成一圈 |
| 图片失败 | 两张纸雕图片均移除 `src` 并隐藏，代码原生月亮、小路与灯光回退场景接管 |

## 5. 规则、隐私与音频边界

- `peakAngle` 只记录达到过的最大净顺时针角度，逆转不会倒扣进度，也不会重复触发旧音；
- 离散“转一格”先对齐历史峰值再增加 `π / 2`，保证每次合法输入恰好产生一个新音；
- 声音只在 `stepIndex` 增加后排队，Pointer、键盘和按钮共用同一状态出口；
- `audioEpoch` 在静音、重开、隐藏和关闭时作废旧异步准备/播放任务，避免上一轮声音落入新一轮；
- 最终留言在完成前不进入 DOM，重开立即删除；页面没有输入框、上传、存储或远程请求。

## 6. 响应式与可访问性

| 视口 / 能力 | 结果 |
| --- | --- |
| 1504×1046 | `scrollWidth/clientWidth = 1504`，`scrollHeight/clientHeight = 1046`；标题、控制、完整音乐盒与摇柄构成单一首屏画面 |
| 390×844 | `scrollWidth = clientWidth = 390`；主按钮 221×54px、摇柄 116×116px，两者水平间隔 21px |
| 320×760 | `scrollWidth = clientWidth = 320`；主按钮 172×54px、摇柄 104×104px，两者水平间隔 14px，旋钮最右边界为 320px |
| reduced motion | 页面和摇臂计算过渡时长均为 `0.00001s`，规则与终局不依赖动画 |
| 键盘 | 摇柄提供 `ArrowRight` / `Space`；另有 48px“转一格”按钮作为明确替代 |
| 语义 | 原生 button/link、H1、八圈进度 list、动态 aria-label 与 live region 均随权威状态更新 |

## 7. 视觉概念与运行截图

- [桌面概念 1504×1046](assets/hand-crank-music-box/concept-desktop.png)；
- [手机概念 853×1844](assets/hand-crank-music-box/concept-mobile.png)；
- [桌面 `file://` 运行截图 1504×1046](assets/hand-crank-music-box/implementation-file-direct.png)；
- [手机运行完整页 390×987](assets/hand-crank-music-box/render-mobile.png)。

运行截图由 Playwright 驱动的本机 headed Chromium 在真实 `file://` 页面截取。桌面截图使用概念原生 1504×1046 CSS 像素；手机概念是高分辨率长图，运行证据按目标设备 390×844 视口截取完整 987px 页面。概念图不在运行时加载。

## 8. 保真账本

| 比较点 | 概念证据 | 运行证据 / 处理 |
| --- | --- | --- |
| 单一主物件 | 打开的木质音乐盒占据画面主体 | 保留唯一大型音乐盒，不增加卡片网格或装饰面板 |
| 午夜色调 | 深靛蓝背景、胡桃木、黄铜与珊瑚红 | 运行页保持同一四组主色，背景不使用渐变或外部纹理 |
| 纸雕夜景 | 两间亮灯小屋由蜿蜒小路相连 | 使用本地原创纸雕资产，并提供代码原生无图回退 |
| 机械语义 | 音筒、音梳、齿窗和右侧摇柄 | 四个结构均保留；运行版简化为可动画、可聚焦的代码原生几何件 |
| 文案层级 | 大号宋体标题、短引导、单一珊瑚主动作 | 桌面双列和手机纵向均保留相同层级与动作优先级 |
| 八圈进度 | 八个黄铜节点与 `0 / 8 圈` | 八节点逐圈点亮，计数由权威步数派生 |
| 移动构图 | 音乐盒在上、进度/动作与摇柄在下 | 390px 保持同序；摇柄右对齐到预留栏，避免覆盖主动作 |
| 动态揭晓 | 纸景随旋律慢慢展开 | 通过裁切比例、亮度和最终留言分层揭晓，不把交互文字烘焙进图片 |

有意偏差：概念中的机械件接近写实产品渲染，运行版使用更扁平的代码原生机械结构，以支持真实旋转、状态着色、缺图回退和缩放；手机概念为 853×1844 视觉稿，真实验证采用 390×844 设备视口并允许短距离自然滚动。核心构图、色彩、纸雕焦点、八圈进度与动作层级保持一致。

## 9. 发现并修复的问题

- 反转后直接在当前角度增加一格，可能只回到旧峰值而不产生新音：离散推进先对齐 `peakAngle`；
- 390px 下居中的摇柄覆盖主按钮，320px 下旋钮再产生 6px 横向溢出：窄屏让摇柄落入右侧预留栏，最窄断点增加 6px 安全区；
- `.sr-only` 的 1px 盒仍参与桌面高度计算并留下滚动残留：补齐经典 `margin: -1px` 裁切约定；
- 原生按钮混入 slider 的 `aria-value*`：保留 button 语义，以动态 `aria-label` 读出圈数和格数；
- 非法角度返回 `NaN`、UI 却只检查 `null`：入口统一拒绝所有非有限增量；
- favicon 404 与图片早于监听器失败会污染/破坏降级：增加空内联 favicon，并同时覆盖已失败与后续 `error` 两条图片路径；
- 自定义 8–16 音动机的活动齿位误用每圈四格取模：改为按实际 `motif.length` 派生并动态生成齿窗；
- “只复制作品目录仍完整有声”的完成定义与统一播放器路径冲突：明确完整仓库可直接打开，便携包需携带共享播放器；
- 应用内 Browser 插件版本漂移：仓库不修改外部插件，以 headed Chromium 完成同等验收。

对应项目 bug 与通用经验已写入 `bugs/` 和 `learn/`。
