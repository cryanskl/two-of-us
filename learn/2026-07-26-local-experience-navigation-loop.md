# 本地体验也需要显式可逆导航

统一门户的本地体验不能只证明“入口能打开”。完整合同应是：

1. 门户卡片唯一且可达；
2. 真实点击进入正确体验；
3. 体验提供可见、可聚焦的返回入口；
4. 相对链接在 localhost 与 `file://` 都指回根页；
5. 正常模式和无脚本降级都不依赖浏览器历史。

浏览器返回键受标签页来源、自动化宿主和历史栈影响，不应成为唯一返回方式。静态
相对 `<a>` 是本地优先页面最可靠的导航原语，也不会把导航状态混入玩法核心。
