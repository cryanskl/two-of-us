# 含内容身份校验的仓库不要用 node_modules 符号链接

多 worktree 项目通常可以用符号链接共用依赖，但如果启动器会遍历资源并拒绝符号
链接，这种做法会让模块测试通过、启动复用测试失败，形成不完整的绿色结果。

更稳妥的顺序：

1. 先确认目标 worktree 是否已有真实依赖目录；
2. 缺失时复用同一锁定安装的本地副本，保持目录为普通文件；
3. 明确依赖目录未跟踪，检查 package 与 lock 没有变化；
4. 除项目定向测试外，必须再跑涉及启动器与内容身份的全仓测试。
