# 四符片名擂台：借鉴声明标题不符合目录协议

- 状态：`fixed`
- 日期：2026-07-26
- 环境：根级 `npm run verify`

## 复现与实际结果

把 `four-symbol-film-duel` 加入 catalog 后运行仓库验收，validator 报告项目
README 缺少独立标题“`## 借鉴与来源声明`”。README 已有完整的独立实现和
零复制说明，但标题写成了近义的“`## 借鉴声明`”。

## 预期

每个已安装作品都使用仓库约定的精确二级标题，让结构化 Gate 能稳定发现并审查
借鉴、来源、许可证与未复制边界。

## 根因与修复

项目 UI 合同只检查借鉴文字，没有锁定共享目录协议要求的标题。现在把 README
标题改为精确值，并在项目测试中新增对应断言；声明正文、来源和权利边界不变。

## 回归

- `node --test experiences/versus/four-symbol-film-duel/*.test.js` 通过；
- `npm run verify` 能把该作品作为 A 级入口完整验收；
- 目录接入测试继续核对零第三方运行依赖、代码和资产。
