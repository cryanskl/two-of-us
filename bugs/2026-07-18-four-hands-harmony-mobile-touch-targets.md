# 这一拍，刚好和你：移动断点把三个交互目标压到 44px

- 状态：`fixed`
- 日期：2026-07-18
- 影响作品：这一拍，刚好和你
- 发现版本 / commit：`2d36851 feat: add four hands harmony experience`

## 环境

- 操作系统：任意
- 浏览器：宽度不超过 760px 的现代浏览器
- 启动等级与入口：A 级，本地静态页面

## 复现步骤

1. 以 390px 或 320px 宽度打开作品。
2. 检查返回链接、声音开关和“暂停合奏”的计算高度。
3. 对照实现规格的 48px 最小触控目标 Gate。

## 预期结果

所有交互控件在窄屏仍至少 48px 高。

## 实际结果

移动断点显式把顶栏、返回链接、声音开关和暂停按钮的最小高度覆盖为 44px，违反已冻结规格。

## 根因

为了压缩 320×700 的纵向布局，移动样式误把主要交互目标从桌面 48px 降为 44px；琴键本身仍保持 58px 以上。

## 解决方案

把顶栏、返回链接、声音开关和暂停按钮的移动最小高度恢复为 48px，不改变字体、横向间距、琴键或滚动策略。

## 回归验证

- [x] catalog 静态 Gate 57 / 57 通过
- [x] 全仓测试 536 / 536，统一验收确认 40 个作品入口
- [x] 390×844 与 320×700 三个目标均为 48px、无横向溢出；320px 仅有允许的 6px 纵向滚动

## 相关提交

- `9ac3ebb fix: restore four hands touch targets`
