# 把这首转给你：共享播放器路径与单目录便携承诺冲突

- 状态：`fixed`
- 日期：2026-07-18
- 影响作品：把这首转给你
- 发现版本 / commit：`60cc669`

## 复现步骤

1. 只复制 `experiences/surprises/hand-crank-music-box/` 到仓库外；
2. 双击 `index.html` 并检查声音脚本路径。

## 预期结果

完成定义与真实依赖边界一致。

## 实际结果

规格声称“作品目录可独立复制并完整游玩”，但页面引用 `../../../shared/audio/tone-player.js`；只复制作品目录会无声降级。

## 根因与解决方案

把“完整仓库直接打开”误写成“单目录可移植”。保留统一共享依赖，并把完成定义改为：完整仓库结构可双击；便携包必须携带共享播放器的相对路径；缺失时仍能无声完成。

## 回归验证

- [x] 完整仓库 `file://` 的播放器路径可解析；
- [x] AudioContext 不可用时仍可完成；
- [x] README、规格与验收对依赖边界表述一致。

## 相关提交

- 验收与沉淀提交
