# 月面供电：发布后创意池仍标记为未实现

- 日期：2026-07-20
- 阶段：下一轮候选审计
- 影响：创意池已实现总数少 1，C08 没有作品链接，下一轮选题会把已完成机制误判为候选
- 状态：已修复

## 复现

1. 打开 `docs/40-idea-backlog.md`；
2. 作品目录和统一门户已经包含“月面，保持有光”；
3. 顶部仍写 33 项已实现、合作 12 项、候选 27 项，C08 仍是无链接纯文本。

## 根因

目录接入阶段更新了统一门户、分类说明和总览计数，但没有把创意池作为同一份发布账本的消费者。现有目录测试只验证门户两条读取路径，不检查创意池的“已实现必须有链接”约定。

## 修复

- 总计改为 34 项，合作改为 13 项，剩余候选改为 26 项；
- C08 链接到 `experiences/co-op/moon-base-power/`，并写明落成名称；
- 设备说明补齐已经实现的触屏路径。

## 回归验证

- `npm run verify` 通过；
- 人工核对总数满足 `11 + 13 + 10 = 34`，剩余 `60 - 34 = 26`；
- C08 相对链接指向已存在的作品目录。

